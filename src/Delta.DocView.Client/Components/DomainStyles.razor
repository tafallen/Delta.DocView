@using Delta.DocView.Client.Services
@inject ClientStepLibraryStore Store

<style>
    :root {
    @foreach (var domain in Store.Domains)
    {
        @:    @(DomainPalette.CssVarName(domain.Id)): @DomainPalette.CssVarValue(domain.Id);
    }
    }
</style>
